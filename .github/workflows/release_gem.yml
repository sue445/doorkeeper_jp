name: Publish gem to rubygems.org

on:
  workflow_dispatch:

jobs:
  release:
    uses: sue445/workflows/.github/workflows/release_gem.yml@main
    with:
      repo-name: sue445/doorkeeper_jp
      gem-name:  doorkeeper_jp
    permissions:
      contents: write
      id-token: write
